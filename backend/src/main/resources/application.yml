spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ibmbank_api
    username: postgres
    password: Th@bata2012
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
